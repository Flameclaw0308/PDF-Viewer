<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
</head>
<body>
  <h1>Welcome to the Dashboard</h1>

  <% if (isAdminUser()) { %>
    <!-- Show the download button for admin users -->
    <button id="downloadButton">Download File</button>
  <% } %>

  <button>hi</button>
  
  <button>hi</button>
  
  <button>hi</button>
  <button>hi</button>
  <button>hi</button>
  <button>hi</button>
  <button>hi</button>

  <script>
    // Add an event listener to the download button
    const downloadButton = document.getElementById('downloadButton');
    if (downloadButton) {
      downloadButton.addEventListener('click', () => {
        if (isAdminUser()) {
          // Redirect to the file download route for admin users
          window.location.href = '/download-file';
        } else {
          // Display an error message for non-admin users
          alert('You are not authorized to download the file.');
        }
      });
    }
  </script>
</body>
</html>
